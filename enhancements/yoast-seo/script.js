/**
 * Script.
 */
jQuery(function ($) {
  // Dashboard widget feed.
  $("#yoast-seo-dashboard-widget .wordpress-feed").remove();

  // Floating save button.
  $("#wpseo-submit-container-fixed.wpseo-admin-submit-fixed").remove();
});
